<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono_cuadrado"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_circular"
        android:supportsRtl="true"
        android:theme="@style/Theme.Veterinaria"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".eliminarcliente"
            android:exported="false" />
        <activity
            android:name=".buscarcliente"
            android:exported="false" />
        <activity
            android:name=".registrocliente"
            android:exported="false" />
        <activity
            android:name=".vistapersonas"
            android:exported="false" />
        <activity
            android:name=".editarcliente"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar.Fullscreen" />
        <activity
            android:name=".listacliente"
            android:exported="false" />
        <activity
            android:name=".dashboard"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar.Fullscreen" />
        <activity
            android:name=".login"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar.Fullscreen" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>